spec:
  ingressClassName: haproxy
  rules:
  - http: # No 'host' line here makes it a "Catch-All"
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: studio-service
            port:
              number: 3004